import 'package:flutter/cupertino.dart';

class TravelNoticeView extends StatelessWidget {
  const TravelNoticeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Travel Notice Page'),
    );
  }
}
